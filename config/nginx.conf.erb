worker_processes 1;
daemon off; # Run in the foreground

events {
  worker_connections 1024;
}

http {
  # --- LOGGING LINES MOVED HERE ---
  # Redirect all logs to Heroku's log stream
  error_log stderr;
  access_log /dev/stdout;
  # --------------------------------

  include       mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  keepalive_timeout 65;

  server {
    # Listen on the port Heroku provides
    listen <%= ENV["PORT"] %>;
    server_name _;
    
    # Set the root directory to your app's root
    root /app;

    # It tells NGINX to use ni-demo.html as the default file
    # for the main "/" URL.
    location / {
      index ni-demo.html;
    }
  }
}

